#include "backend/network/JsonRequest.h"

JsonRequest::JsonRequest(const User& user, const QString& category, const int from, const int to)
{
    //document
    Q_UNUSED(user)
    Q_UNUSED(category)
    Q_UNUSED(from)
    Q_UNUSED(to)

    //setup document..
}
